const AuthenticationConfiguration = require('./authentication');
const DatabaseConfiguration = require('./database');
const ServerConfiguration = require('./server');
const HashingServiceConfiguration = require('./hashing');

module.exports = {
  AuthenticationConfiguration,
  DatabaseConfiguration,
  HashingServiceConfiguration,
  ServerConfiguration,
};
